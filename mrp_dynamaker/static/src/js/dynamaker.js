//odoo.define('dynamaker.dynamaker_product', function (require) {
//'use strict';

//var core = require('web.core');
//var config = require('web.config');
//var publicWidget = require('web.public.widget');
//var VariantMixin = require('sale.VariantMixin');
//var rpc = require('web.rpc');
//var qweb = core.qweb;

//publicWidget.registry.WebsiteSale = publicWidget.Widget.extend(VariantMixin, {
    //selector: '.oe_website_sale',
    //events: _.extend({}, VariantMixin.events || {}, {
        //'click .a-submit-dynamaker': '_onClickSubmit',
    //}),
    //_onClickSubmit: function (ev) {
	//console.log("submitting....");
    //var iframeWin = document.getElementById("dynamaker-configurator").contentWindow;
    //iframeWin.postMessage("send event to dynamaker", "https://deployed.dynamaker.com");
    //},
//});
//});
